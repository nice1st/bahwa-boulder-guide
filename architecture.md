# 바위 볼더링 가이드 — 아키텍처 문서

## 뷰잉 (일반 사용자)

### 지도
- 마커 종류별 아이콘 구분 (볼더/주차장/화장실/갈림길)
- 볼더 마커: 썸네일 포함, GPS 좌표 표기
- 줌 인/아웃에 따라 마커 클러스터링 (겹침 처리)
- **필터**: V 난이도 (복수 선택, V10+ 묶음), 최소 패드 수
- 어프로치 경로(Path) 폴리라인 — 기본 숨김
- 내 위치 버튼 (GPS)
- 뒤로가기(popstate) — 루트 상세 → 마커 패널 → 지도 순서로 단계적 닫기
- 상단바 — 앱 이름, 햄버거 메뉴 (로그인/로그아웃, admin만 관리자 링크)

### 마커 클릭
- 바텀시트 — 루트 목록 + 사진 슬라이드
- "어프로치" 버튼 → 경로 폴리라인 토글

### 루트 클릭
- 영상 링크 전체 목록
- 댓글 목록 + 댓글 작성 (비로그인)

---

## 관리자 기능

- `/admin` 경로, Google OAuth + role=admin 체크
- PC 친화 레이아웃: 좌(지도 50%) + 우(목록+편집 50%)
- 사용자 지도와 동일한 KakaoMap 컴포넌트 공유 (mode='admin')

### 마커 관리
- 사진 업로드 → EXIF GPS 자동 추출 → 50m 반경 감지
- 지도 클릭으로 좌표 지정
- 마커 종류 선택 (볼더/주차장/화장실/갈림길)

### 루트 관리
- 볼더 마커에 루트 추가 (이름, V난이도, 설명, 사진, 영상 링크)

### 경로(Path) 관리
- 지도에서 마커/좌표 순서대로 클릭 → 폴리라인 생성

### 사용자 관리
- 사용자 목록 조회, 역할 승격/강등 (user/admin)

---

## 할 일

### 버그
- [x] 어드민 지도에서 마커 첫 클릭 시 줌인 이상 — setCenter+setLevel 즉시 실행 + renderMarkers 재호출로 해결

### 개선
- [x] 어드민 마커 목록 영역 너무 좁음 — 드래그 리사이즈 bar로 변경
- [x] 비볼더 마커 클러스터링 제외 — 줌 레벨 5 이하에서만 표시, 볼더만 클러스터링
- [x] 내 위치 버튼 개선 — 클릭 시 줌인 + 현재 위치 파란 점 표시
- [x] 이미지 넘기기 애니메이션 — 바텀시트/풀스크린 뷰어에서 슬라이드 전환 시 가벼운 트랜지션
- [x] 이미지 더블터치 확대 — 풀스크린 뷰어에서 더블탭으로 줌인/줌아웃 + 드래그 이동

### 모바일 버그
- [x] 하단 패널 열린 후 첫 드래그를 지도가 가져감 — 패널에 stopPropagation 추가
- [x] 패널 있는 상태에서 지도 터치+이동해도 패널 안 닫힘 — 네이티브 DOM 이벤트 리스너로 교체
- [x] 이미지 전환 애니메이션 개선 — 갤러리 스타일 슬라이드 (드래그 따라가기 + translateX)
- [x] 더블탭 줌아웃 불완전 — 줌아웃 시 offset 완전 리셋, 핀치줌은 브라우저 위임
- [x] 뒤로가기 시 단계적 닫기 — usePopstate에 enabled 파라미터 추가, 최상위 컴포넌트만 popstate 처리
- [x] 마커→패널→루트 뒤로가기 꼬임 — usePopstate enabled 조건부 활성화로 해결
- [x] 루트 상세에서 이미지 닫기 버튼 없음 — z-index 60 + stopPropagation으로 항상 보이도록

### 어드민 버그/개선
- [x] 마커/루트 추가·수정을 팝업(모달)으로 변경
- [x] 볼더가 아닌 마커 선택 시 하위 루트 관리 숨기기

### 할 일
- [x] 비볼더 마커 크기 축소 — 볼더 대비 0.6배 (17px)
- [x] 어드민 마커 목록을 현재 지도 뷰포트 내 마커만 필터링 — onBoundsChange 콜백
- [x] 어드민 경로 탭에도 지도 추가 — 좌측 지도 + 우측 PathManager 레이아웃

### 미결
- [ ] 댓글 스팸 방지 (비로그인 — 추후 결정)
